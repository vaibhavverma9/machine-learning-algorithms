This is an implementation of k-means and k-means++ in python. 

The python file runs k-means++ by default.
The text file toydata.txt contains data points. 
Libraries used include pyplot, numpy, random and math. 
